<h2>SORRY!</h2>
<p><b>(You are not authorized to access this photo please contact the owner.)</b></p>

<h2>You attempted to login with an unauthenticated user, the user has been listed below</h2>
<% @tokens.each do |token| %>
  <% token.info = JSON.parse(token.info) %>
  <p><%=link_to(token.provider+"/" + (token.info['user'] || token.info['email']), '/'+token.provider+"/"+token.id.to_s)%></p>
<% end %>
